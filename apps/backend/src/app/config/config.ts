import { BCRYPT_CONFIG } from './bcrypt';
import { FS_PATH_CONFIG } from './fs-path';
import { MONGODB_CONFIG } from './mongodb';

export const configuration = [MONGODB_CONFIG, BCRYPT_CONFIG, FS_PATH_CONFIG];
